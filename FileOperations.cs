using System;
using System.Collections.Generic;
using System.IO;
using Avalonia.Platform.Storage;
using System.CodeDom.Compiler;

namespace AOP_Homework;
class FileOperations
{
    // protected internal char[][] LoadFile(string file)
    // {
    //     MainWindow mainWindow = new MainWindow();
    //     using StreamReader sr = new StreamReader(file);
    //     string[] line = sr.ReadLine().Split(' ');

    //     int height = int.Parse(line[0]);
    //     int width = int.Parse(line[1]);
    //     mainWindow.SetupCanvas(height, width);
    //     char[] chardata = sr.ReadToEnd().ToCharArray();
    //     char[][] data = new char[height][];
    //     for (int i = 0; i < height; i++)
    //     {
    //         data[i] = new char[width];
    //         for (int j = 0; j < width; j++)
    //         {
    //             data[i][j] = chardata[i * width + j];
    //         }
    //     }
    //     sr.Close();
    //     return data;
    // }

  
}